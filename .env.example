# Backend API base URL (includes the /api/v1 prefix used by every controller)
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Required for the outer venue location map (components/map/VenueLocationMap.jsx)
VITE_GOOGLE_MAPS_API_KEY=
